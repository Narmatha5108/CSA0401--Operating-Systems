#include <stdio.h>
#include <string.h>

int main()
{
    FILE *fp;
    char filename[50];
    char pattern[50];
    char line[200];

    printf("Enter file name: ");
    scanf("%s", filename);

    printf("Enter pattern to search: ");
    scanf("%s", pattern);

    fp = fopen(filename, "r");

    if (fp == NULL)
    {
        printf("File not found.\n");
        return 1;
    }

    printf("\nLines containing '%s':\n", pattern);

    while (fgets(line, sizeof(line), fp) != NULL)
    {
        if (strstr(line, pattern) != NULL)
        {
            printf("%s", line);
        }
    }

    fclose(fp);

    return 0;
}



OUTPUT:

Enter file name: sample.txt
Enter pattern to search: UNIX

Lines containing 'UNIX':
UNIX commands
UNIX operating system
